{
  "version": "2.0.0",
  "tasks": [
    {
      // This will run yarn install
      "label": "yarn install",
      "type": "shell",
      "command": "corepack enable && yarn install",
      "problemMatcher": ["$tsc"],
      "group": {
        "kind": "build"
      },
      "presentation": {
        "close": true,
        "group": "build",
        "reveal": "never"
      }
    }
  ]
}
