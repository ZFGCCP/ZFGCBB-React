{
  "version": "2.0.0",
  "tasks": [
    {
      // This will run yarn install
      "label": "yarn install",
      "type": "shell",
      "command": "bash",
      "args": ["-c", "'corepack enable && yarn install'"],
      "problemMatcher": ["$tsc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "close": true,
        "group": "build",
        "reveal": "never"
      }
    }
  ]
}
