export type Theme = {
  borderWidth: string;

  backgroundColor: string;
  widgetColor: string;
  tableRow: string;
  tableRowAlt: string;
  textColor: string;
  black: string;
};

export type ThemeWrapper = {
  currentTheme: Theme;
};
